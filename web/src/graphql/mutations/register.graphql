mutation Register($data: RegisterInput!) {
  register(data: $data) {
    id
    userId
    user {
      id
      firstName
      lastName
      name
      email
    }
    statments {
      operationType
      amount
      description
    }
  }
}
